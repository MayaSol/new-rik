//- Все примеси в этом файле должны начинаться c имени блока (order-info)

mixin order-info(props={}, mods)

  -
    // список модификаторов
    var allMods = '';
    if(typeof(mods) !== 'undefined' && mods) {
      var modsList = mods.split(',');
      for (var i = 0; i < modsList.length; i++) {
        allMods = allMods + ' order-info--' + modsList[i].trim();
      }
    }

  .order-info(class=allMods)&attributes(attributes)
    block
